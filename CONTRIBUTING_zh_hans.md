# 为 kristal-i18n 贡献翻译

感谢你愿意贡献！🌍

## 重活已经干完了

创建这个库的时候，我把最麻烦的部分都提前做完了——**英文和简体中文的内置文本都已提取并接入 hook**：

- **所有内置文本都已提取。** 原版 Kristal 的全部文本——对话、过场、选项、物品、武器、防具、法术、菜单、存档界面、商店、Debug 菜单、战斗结算、光世界/暗世界 UI——都已经从引擎里抽出来，放进了 `lang/en.json` 和 `lang/zh_hans.json`，全部接好 hook，拿来就能翻。
- **所有 hook 都已写好。** 你完全不需要碰 Lua 代码。任何会出现文字的地方，都已经接上了本地化系统。
- **简体中文就是完整的参考实现。** `lang/zh_hans.json` 覆盖了所有已接入 hook 的文本，随库发布——任何时候你都有个「翻完了应该长什么样」的完整范例可以参考。

所以贡献者需要做什么？**只需要翻译。** 复制任意一份已有的语言文件，把值翻译成你的语言，在配置里注册一下——完事儿。玩家马上就能在游戏内的**设置菜单**里切换到你的语言。

我全力支持并欢迎为你的语言提交翻译 Pull Request！这正是我创建这个本地化库的初衷——让每个人都能用自己的母语制作游戏、翻译现有的游戏。

> **本指南的侧重：框架内置所有文本**——引擎自己显示的全部文字。如果你想翻译的是原版 Kristal 模板的内容（即演示模组），请看 [kristal-i18n-example 的贡献指南](https://github.com/Bli-AIk/kristal-i18n-example/blob/main/CONTRIBUTING_zh_hans.md)。

## 添加一门新语言

1. **挑一份起点文件。** 复制任意一份已有的语言文件，命名为 `lang/<你的语言>.json`（比如法语 `lang/fr.json`、日语 `lang/ja.json`）。`lang/zh_hans.json` 是最完整的参考，也可以从你读起来最顺的语言开始。
2. **翻译所有值。** 把每个值翻译成你的语言。**键名不要动**——游戏按键名查找文本，所有语言共用同一套键。
3. **注册语言。** 在 `lib.json`（或你模组的 `mod.json` 里的 `config.kristalI18n`）中，把语言 ID 加进 `languages`，把显示名加进 `languageNames`：

   ```json
   "languages": ["en", "zh_hans", "fr"],
   "languageNames": {
       "en": "English",
       "zh_hans": "简体中文",
       "fr": "Français"
   }
   ```

4. **（可选）姓名和资源。** 在 `lang/names.json` 里为你的语言添加角色名；如果语言需要，还可以把贴图/字体/音频覆盖放到 `lang/<你的语言>/...` 路径下（见 README 的「更多技巧」一节）。
5. **游戏内测试。** 演示模组 [kristal-i18n-example](https://github.com/Bli-AIk/kristal-i18n-example) 覆盖了所有 hook——对话、战斗、商店、存档界面、Debug 菜单、光世界。按切换键（默认 `F7`）可以随时来回切换语言检查效果。
6. **提交 Pull Request。** 🎉

## 翻译注意事项

- **键名永不变，只翻译值。** 改键名会让所有语言都查不到文本。
- **键名用稳定 ID。** 不要把带标点、换行或富文本标签的整句显示文本直接当成语言文件的键。
- **保留占位符。** `{key}` 内插和 `[var:name]` / `[name:kris]` 标记必须保留。可以按你的语法调整它们在句中的位置，但不能丢掉。
- **贴合语气。** 原版文本短而紧凑，是配合打字机节奏的。翻译也要保持简洁，才能在游戏里读起来顺畅。
- **`cjk*` 配置只针对中文。** 字间距、偏移这些设置是为了中文字形排版做的；其他语言一般不需要。
- **注意语境。** 有些键在多个界面共用。拿不准的时候，看看 `lang/zh_hans.json` 怎么翻的，或者跑一遍演示模组看实际效果。

## 物品字段怎么填

物品这部分记住几条就够了：

- `item_<id>_name`：物品平时显示的名称。
- `item_<id>_useName`：只有战斗里要显示另一个名字时才写。没写就使用当前语言的 `name`；大小写不同也算不同，英文里的大写名不要删。
- `item_<id>_description`：物品菜单里的说明。
- `item_<id>_check`：在光世界里选择“查看”时显示的文字。它不是 `description` 的备用字段：有的物品两者碰巧一样，有的会多出攻击/防御、换行或其他内容。原文分别有这两个字段，就分别翻译；原文没有，就不要自己补。
- `item_<id>_effect`：战斗物品栏里的短效果，例如回复多少 HP。原文为空时就保持为空。
- `item_<id>_shopName` 和 `item_<id>_shopDesc`：商店里的文字，不能拿 `name` 或 `description` 代替。
- 章节和特殊后缀表示覆盖。战斗、丢弃、反应文字如果有通用备用值，也只有在原文确实不同的时候才写物品专属键；如果代码直接引用了某个专属键，即使内容看起来一样，也要保留。

## 其他贡献方式

- **报告未翻译文本**——如果游戏里出现了没被翻译的原文，发 Issue，附上截图和复现步骤。
- **改进现有翻译**——措辞、语气、一致性修正都欢迎。
- **提建议**——新 hook、新配置项、文档。
- **代码贡献**——hook 覆盖、性能、工具链。动手前先读 README 了解库的设计目标。

## 许可证

提交贡献即表示你同意贡献以 MIT License 和 Apache License, Version 2.0 双许可（与项目一致）。
